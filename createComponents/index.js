const {createComponentsSubDirs} = require('./createComponentsSubDirs')
const {createComponents} = require('./createComponents')
const {createComponentsIndex} = require('./createComponentsIndex')

module.exports = {
  createComponentsSubDirs,
  createComponents,
  createComponentsIndex
}
